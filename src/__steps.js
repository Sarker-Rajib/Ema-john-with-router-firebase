/**
 * 1. create firebase projectId
 * 2. enable web 
 * 3. enable sign in method
 * 4. install firebase
 * 5. get firebse configuration
 * 6. ecport app from firebaseConfig
 * 
 */

/**
 * 1. create UserContext (AuthContext): userContext --> component name, UserContext Provides AuthContext
 * 2. Create auth context (AuthContext)
 * 3. set authcontext provider to firebase
 * 4. make sure you set the children
 * 5. export auth context to be used inside useContext
 * 6. get form data 
 * 7. getAuth in the userContext
 */


/**
 * Firebase Hosting
 * // onxe for each computer
 * 
 * 1. npm install -g firebase-tools
 * 2. firebase login
 * 3. init for each project
 * 4. make sure public directory you set "build"
 * 5. for every deploy npm run build
 * 6. firebase deploy
 */